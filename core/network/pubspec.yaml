name: core_network
description: This package provides network operations.
version: 1.0.0
publish_to: "none"

environment:
  sdk: 3.3.3

dependencies:
  core_common:
    path: ../common
  core_model:
    path: ../model

dev_dependencies:
  core_testing:
    path: ../testing
  test: 1.25.2
  yumemi_lints: 1.7.0
