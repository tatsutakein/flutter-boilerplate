name: core_ui
description: This package provides a UI that depends on the model class and is used by all packages.
version: 1.0.0
publish_to: "none"

environment:
  sdk: 3.3.3
  flutter: 3.19.5

dependencies:
  core_common:
    path: ../common
  core_designsystem:
    path: ../designsystem
  core_model:
    path: ../model
  flutter:
    sdk: flutter

dev_dependencies:
  core_testing:
    path: ../testing
  flutter_test:
    sdk: flutter
  yumemi_lints: 1.7.0

flutter:
