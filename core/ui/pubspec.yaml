name: core_ui
description: This package provides a UI that depends on the model class and is used by all packages.
version: 1.0.0
publish_to: "none"

environment:
  sdk: 3.2.6
  flutter: 3.16.9

dependencies:
  core_common:
    path: ../common
  core_designsystem:
    path: ../designsystem
  core_model:
    path: ../model
  flutter:
    sdk: flutter

dev_dependencies:
  core_testing:
    path: ../testing
  flutter_test:
    sdk: flutter
  yumemi_lints: 1.6.0

flutter:
